import { AppPage } from "./app.po";

describe("SignupComponent Form", () => {
  let page: AppPage;

  beforeAll(() => {
    page = new AppPage();
  });

  // TODO: Add tests when auth bypass is available

  // it("should allow the user to add a new task", () => {});

  // it("should display task details in a popover when task is clicked", () => {});

  // it("should allow the user to delete a task from within popover", () => {});
});
